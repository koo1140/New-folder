# This is outdated.
# Go to the new repo
# https://github.com/koo1140/AI-Agent
---
### AI Agent  
You will need a free Mistral API key https://console.mistral.ai/  
You will need Python.  

### Install dependencies  
```bash
pip install mistralai colorama requests
```
Clone the project
In terminal:
```
git clone [https://github.com/koo1140/New-folder](https://github.com/koo1140/New-folder)
cd New-folder
```
Set API key
🐧 Linux / macOS:
`export MISTRAL_API_KEY="your_key_here"`

🪟 Windows (PowerShell):
`setx MISTRAL_API_KEY "your_key_here"`

You will need to restart the terminal
Run the agent
python main.py

Features
If everything is configured correctly, the agent starts and can:
✔ do web search
✔ execute tool calling
✔ store conversations
✔ launch sub-agents dynamically
✔ make real decisions based on context
✔ Interact with the terminal (Shell)
https://github.com/koo1140/New-folder
